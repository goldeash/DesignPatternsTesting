{
    "ApiSettings": {
        "BaseUrl": "https://lecture-books-api.azurewebsites.net",
        "AuthUrl": "https://login.microsoftonline.com/{tenant_id}/oauth2/v2.0/token",
        "ClientId": "your_client_id_here",
        "ClientSecret": "your_client_secret_here", //гит не даёт пушить реальный сикрет кей так что вот тестовый
        "Scope": "api://{application_id}/.default" 
    }
}